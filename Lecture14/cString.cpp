#include <iostream>
#include <cstring>
using namespace std;

int main() {
    char str1[100] = "hello ";
    char str2[100] = "ankit";

    //copy
    // strcpy(str1, "codewithakki");

    //concatenate
    // strcat(str1, str2);

    //compares
    cout << strcmp(str1, str2) << endl;

    // cout << str1 << endl; 
    return 0;
}
